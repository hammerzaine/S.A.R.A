# Local state / secrets — never commit
credentials.json
config.json
upgrade_state.json
data/
backups/
__pycache__/
*.pyc
*.bak*

# build / environment artifacts
.venv/
dist-clean/
audit-report.txt
audit_full.py

# user-requested exclusion
link.txt

# demo site (not part of agent package)
se-demo-site/
