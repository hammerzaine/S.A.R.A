# Local state / secrets — never commit
credentials.json
config.json
upgrade_state.json
data/
backups/
__pycache__/
*.pyc
*.bak*

# user-requested exclusion
link.txt

# demo site (not part of agent package)
se-demo-site/
